@page "/"
@using BlazorSerial.Services
@inject SerialPortService SpService;

<PageTitle>Blazor Serial</PageTitle>

@if (!SpService.GetSerialPorts().Any())
{
    <p1>Loading serial ports, please wait...</p1>
}
else 
{
    @* <TabControl> *@
    @*     @foreach (var sp in SpService.GetSerialPorts()) *@
    @*     { *@
    @*         <TabPage SerialPort=@sp/> *@
    @*     } *@
    @* </TabControl> *@
    <SerialTabs/>
}